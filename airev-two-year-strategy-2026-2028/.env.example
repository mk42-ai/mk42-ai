# Copy to .env (never commit .env). The assistant reads the OnDemand key from the environment only.
ONDEMAND_API_KEY=
# OnDemand agent (chat project) whose system prompt embeds the data room
ONDEMAND_AGENT_ID=6a9c566598ed33a866ffbf13
# When ONDEMAND_API_KEY is absent locally, /api/* is proxied to the deployed Vercel API
API_PROXY_BASE=https://mk42-ai-git-feature-two-year-strateg-19d701-schoolhack-web-team.vercel.app
PORT=4173
